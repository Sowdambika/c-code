#include <stdio.h>

int main()
{
    int x=5;
    printf("x=%d\n",x);
    x+=20;
    printf("x=%d\n",x);
    x-=4;
    printf("x=%d\n",x);
    x*=10;
    printf("x=%d\n",x);
    x/=3;
    printf("x=%d\n",x);
    x%=3;
    printf("x=%d\n",x);
    return 0;
}
