#include <stdio.h>

int main()
{
    int number,sum=0,digit;
    printf("enter the number\n");
    scanf("%d",&number);
    while(number!=0){
        digit=number%10;
        sum=sum+digit;
        number=number/10;
    }
    printf("total sum of digits in number is: %d",sum);
    return 0;
}
