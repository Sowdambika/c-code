#include <stdio.h>

int main()
{
    int j,i,n,mid;
    printf("enter n value:");
    scanf("%d",&n);
    mid=(n/2)+1;
    for(i=1;i<=n;i++)
    {
        for(j=1;j<=n;j++)
        {
            printf(" %d",i);
        }
        printf("\n");
    }
    return 0;
}
