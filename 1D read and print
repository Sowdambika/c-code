#include <stdio.h>

int main()
{
    int x[10],n;
    printf("enter number of elements:");
    scanf("%d\n",&n);
    
    printf(" enter array elements:");
    for(int i=0;i<n;i++)
    scanf("%d",&x[i]);
    
    for(int i=0;i<n;i++)
        printf("%d\n",x[i]);

    return 0;
}
