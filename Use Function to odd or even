#include <stdio.h>
int is_even(int n)
{
    if(n%2==0)
    return 1;
    else
    return 0;
}
int main()
{
 int i;
 
 for(int i=0;i<=10;i++)
 if(is_even(i))
 printf("%d is even\n",i);
 else
 printf("%d is odd\n",i);
 return 0;
 }
