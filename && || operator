#include <stdio.h>

int main()
{
    int age;
    printf("enter age of the candidate: ");
    scanf("%d",&age);
    if((age>=18)&&(age<=25))
    printf("you are eligible");
    if(age<18 || age>25)
    printf("you are not eligible");
    return 0;
}
