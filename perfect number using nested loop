#include <stdio.h>

int main()
{
    int n,i,sum;
    printf("enter n value:");
    scanf("%d",&n);
    
    //for(n=1;n<=20;n++)
    sum=0;
    for(i=1;i<n;i++)
    {
        if(n%i==0)
            sum=sum+i;
    }
    if(sum==n)
    printf("%d\n",n);
    return 0;
}
