#include <stdio.h>

int main()
{
    int number,count=0,quotient,reminder;
    printf("enter the number\n");
    scanf("%d",&number);
    do{
        number=number/10;
        count++;
    }while(number!=0);
    printf("total number of digits in number is: %d",count);

    return 0;
}
